---
---

Single-source the AI instruction files' Hard Rules via `tools/gen-ai-docs.mjs`, so `CLAUDE.md`, `AGENTS.md` and `.github/copilot-instructions.md` stay byte-identical instead of drifting. Docs and tooling only — no change to the published extension.
